# Direct dependencies of DE2TR.
# Environment used for the released results:
#   Python 3.7.16, PyTorch 1.12.1+cu113 (CUDA 11.3), single RTX 3090.
# The PyTorch wheel is CUDA-build specific, so install it first, e.g.
#   pip install torch==1.12.1+cu113 torchvision==0.13.1+cu113 \
#       --extra-index-url https://download.pytorch.org/whl/cu113
torch>=1.9.0
numpy==1.21.5
scipy==1.7.1
scikit-learn==0.24.2
pandas==1.3.1
tabulate==0.9.0        # pandas.DataFrame.to_markdown in utils/basic_utils.py
tqdm==4.67.1
tensorboard==2.5.0
easydict               # only imported by de2tr/text_encoder.py
